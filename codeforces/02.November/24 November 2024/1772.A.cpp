#include <bits/stdc++.h>
using namespace std;

int main()
{
    int a, b, t;
    char ch;

    cin >> t;

    while (t--)
    {
        cin >> a >> ch >> b;
        cout << (a + b) << endl;
    }

    return 0;
}